# HCMUE-Tools

Frontend: Vite + React in `frontend/`
Backend: Express + TypeScript in `backend/`

## Deploy

- Set `VITE_API_BASE_URL` for the frontend on Vercel.
- Set `HCMUE_API_KEY` and `PORT` for the backend on Render.
- Run `npm run build` in both apps before publishing.
